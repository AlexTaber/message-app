<%= form_for my_request, html: { class: 'typeahead-form', id: "request-typeahead" } do |f| %>
  <%= text_field_tag :site_id, nil, class: "typeahead app-view-typeahead request-typeahead", value: "", placeholder: "Search Mercury Sites" %>

  <%= f.hidden_field :site_id, value: nil %>
  <%= f.hidden_field :user_id, value: current_user.id %>
<% end %>