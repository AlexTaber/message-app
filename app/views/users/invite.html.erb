<%= form_tag invite_path, method: "POST" do %>
  <%= collection_select(:site, :site_id, current_user.sites, :id, :name) %>
  <div><%= text_field_tag :email, nil, placeholder: "Email" %></div>
  <div><%= submit_tag 'Send Invite', class: "button button-primary" %></div>
<% end %>